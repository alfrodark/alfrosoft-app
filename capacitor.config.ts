import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alfrosoft.alfrosoftapp',
  appName: 'alfrosoft-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
